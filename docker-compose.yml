services:
  visiondata-front:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - node_modules:/app/node_modules

volumes:
  node_modules: